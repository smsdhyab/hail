-- 0033_regrant_line_total.sql — إعادة منح line_total بعد إعادة بنائه
--
-- إسقاط عمود يُسقط معه منحه. أعيد بناء line_total في 0030 و0031 (لأنه عمود
-- محسوب ولا يمكن تعديل تعبيره)، فسقط منح القراءة عنه بصمت — واستعلام الكاشير
-- يطلبه، ففشل الاستعلام كاملاً وظهرت الطلبات **بلا أصناف** على الشاشة.
--
-- الخطأ لم يظهر إلا في التشغيل الحقيقي: مفتاح الخدمة يتجاوز المنح، فكل اختبار
-- عبر واجهة الإدارة كان ينجح.
grant select (line_total) on public.order_items to authenticated;
